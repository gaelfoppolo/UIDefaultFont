# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
osx_image: xcode8.3
# cache: cocoapods
# podfile: Example/Podfile

# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example

script:
  - set -o pipefail && xcodebuild test -workspace Example/UIDefaultFont.xcworkspace -scheme UIDefaultFont-Example -sdk iphonesimulator -destination 'OS=10.3.1,name=iPhone 7 Plus' | xcpretty
  - pod lib lint
after_success:
  - bash <(curl -s https://codecov.io/bash)
