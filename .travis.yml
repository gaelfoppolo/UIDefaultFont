# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

os: osx
osx_image: xcode9
language: objective-c
# cache: cocoapods
# podfile: Example/Podfile

# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example

script:
  - set -o pipefail && xcodebuild test -workspace Example/UIDefaultFont.xcworkspace -scheme UIDefaultFont-Example -sdk iphonesimulator -destination 'OS=11.0,name=iPhone 7 Plus' | xcpretty
  - pod lib lint
after_success:
  - bash <(curl -s https://codecov.io/bash)
